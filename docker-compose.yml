version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Note: DEBUG logs include full prompt/schema context and should stay off
    # by default. Set DEBUG=true in your .env if you need verbose logging.
